# Direct test/dev dependencies. This file is the source of truth for humans;
# CI installs from the resolved, hash-pinned requirements_test.lock instead.
# After changing anything here, regenerate the lock (command in its header):
#
#   uv pip compile requirements_test.txt --generate-hashes \
#     --only-binary :all: --no-binary mock-open,pyric \
#     --python-version 3.14.2 -o requirements_test.lock
#
# pytest-homeassistant-custom-component pins the matching Home Assistant
# release (0.13.337 -> homeassistant 2026.6.1, which requires Python >= 3.14.2).
homeassistant==2026.6.1
pytest-homeassistant-custom-component==0.13.337
pymodbus>=3.5.0
paho-mqtt>=2.0.0
# Used by scripts/debug_dtu.py for CLI + TOML config-file parsing.
ConfigArgParse>=1.7
pytest
pytest-asyncio
# Pinned: ruff's default rule set / noqa handling changes between releases, which
# otherwise breaks the scheduled CI run on every new ruff version.
ruff==0.16.1
black
isort
flake8
flake8-typing-imports
mypy
pre-commit
